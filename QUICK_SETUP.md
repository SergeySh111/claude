# Быстрая настройка связи с GitHub и Railway

## Шаг 1: Перезапустите терминал

После установки Git **обязательно перезапустите PowerShell/терминал**, чтобы Git появился в PATH.

## Шаг 2: Запустите скрипт настройки

```powershell
cd c:\Users\sergey.sh\Downloads\campaign-analyzer
.\setup-github-railway.ps1
```

Скрипт автоматически:
- ✅ Проверит наличие Git
- ✅ Инициализирует репозиторий (если нужно)
- ✅ Добавит все файлы
- ✅ Создаст коммит
- ✅ Подключит к GitHub (попросит URL репозитория)
- ✅ Отправит код на GitHub

## Шаг 3: Создайте репозиторий на GitHub

Если репозиторий еще не создан:

1. Откройте https://github.com/new
2. **Название**: `campaign-analyzer`
3. **Описание**: "Campaign Performance Analyzer with AI-powered insights"
4. Выберите **Public** или **Private**
5. **НЕ добавляйте** README, .gitignore или лицензию (у нас уже есть)
6. Нажмите **"Create repository"**

После создания скопируйте URL репозитория (например: `https://github.com/your-username/campaign-analyzer.git`)

## Шаг 4: Подключите к Railway

1. Откройте https://railway.app
2. Войдите в аккаунт (через GitHub)
3. **New Project** → **Deploy from GitHub repo**
4. Выберите репозиторий `campaign-analyzer`
5. Railway автоматически начнет деплой

## Шаг 5: Настройте переменные окружения в Railway

В Railway Dashboard → **Variables**, добавьте:

```
OPENAI_API_KEY=sk-ваш-ключ-от-openai
NODE_ENV=production
```

**Важно**: После добавления переменных Railway автоматически перезапустит деплой.

## Готово! 🎉

Теперь:
- ✅ Каждый `git push` автоматически задеплоит на Railway
- ✅ Изменения в коде → коммит → push → автоматический деплой
- ✅ Railway будет использовать `nixpacks.toml` для сборки

## Быстрые команды для работы:

```powershell
# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "описание изменений"

# Отправить на GitHub (и автоматически задеплоить на Railway)
git push

# Посмотреть логи Railway
# (через Railway Dashboard → Deployments → View Logs)
```

## Troubleshooting

### Git не найден
- Перезапустите терминал
- Или добавьте Git в PATH вручную

### Ошибка авторизации GitHub
- Используйте **Personal Access Token** вместо пароля
- Или настройте SSH ключи

### Railway не деплоит
- Проверьте логи в Railway Dashboard
- Убедитесь, что переменные окружения установлены
- Проверьте, что `nixpacks.toml` в репозитории
